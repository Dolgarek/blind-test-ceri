{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="littleContainer">
        <button class="btn btn-mybtn">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
