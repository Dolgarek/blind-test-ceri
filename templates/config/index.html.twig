{% extends 'base.html.twig' %}

{% block title %}Configuration de la partie{% endblock %}

{% block body %}
    <div class="container-fluid m-0">
        <div class="row mt-4">
            <div class="col-12 mt-4 pt-4">
                <h2 class="text-center">Configuration de la partie</h2>
            </div>
            <div class="row">
                <div class="col-12 d-flex justify-content-center">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary mt-3">{{ button_label|default('Start') }}</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
